version: 2
models:
- name: orders
  columns:
  - name: shipping_address
    meta:
      masking_policy: mask_address
      justification: Shipping address contains physical location details which is
        classified as personally identifiable information (PII).

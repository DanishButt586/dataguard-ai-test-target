version: 2
models:
- name: orders
  columns:
  - name: shipping_address
    meta:
      masking_policy: mask_address
      justification: Contains physical location details which constitutes Personally
        Identifiable Information (PII).

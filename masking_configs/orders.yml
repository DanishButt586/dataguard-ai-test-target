version: 2
models:
- name: orders
  columns:
  - name: shipping_address
    meta:
      masking_policy: mask_address
      justification: Contains physical delivery address details which are considered
        personally identifiable information (PII).
